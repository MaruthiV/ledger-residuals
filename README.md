# Ledger Residuals

A two-stream transformer residual architecture: a mutable **Deliberation** stream (erasable, delta-rule updates) and a protected, decode-only **Commitment** stream, coupled by a one-directional commit gate.

Research code for an ML paper in progress.
